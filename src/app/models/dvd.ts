export interface Dvd {
    id?: number;         // Unique identifier
  title: string;      // DVD title
  genre: string;      // Genre of the DVD
  director: string;   // Director of the DVD
  image: string; 
}
