import { PendingFilPipe } from './pending-fil.pipe';

describe('PendingFilPipe', () => {
  it('create an instance', () => {
    const pipe = new PendingFilPipe();
    expect(pipe).toBeTruthy();
  });
});
